# Backend origin this frontend talks to. Set this per deployment environment
# (e.g. in Vercel's Production env vars) rather than adding a *_PROD sibling —
# Vite/Vercel already scope env vars per environment.
VITE_API_URL=http://localhost:5000

VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
